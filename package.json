{
  "scripts": {
    "dev": "cd frontend && vite",
    "build": "cd frontend && npm install && npm run build",
    "preview": "cd frontend && vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
